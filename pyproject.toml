[tool.poetry]
name = "protest"
version = "0.1.0"
description = ""
authors = ["Benjamin  Shaw <benjamin.shaw@manchester.ac.uk>"]
readme = "README.md"
packages = [{include = "protest", from = "src"}]

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^1.24.2"
requests = "^2.28.2"
pytest-mock = "^3.10.0"
pytest = "^7.2.2"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
