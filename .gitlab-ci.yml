default:
    image: python:3.6

stages:
    - test

before_script:
    - pip install --upgrade pip
    - pip install poetry
    - pip install --upgrade poetry
    - poetry install
    - source `poetry env info --path`/bin/activate

test:
    stage: test 
    script:
      - which python
